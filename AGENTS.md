# AGENTS.md

## 1. この文書の目的

この文書は、DRRシステムを設計・実装・レビューするエージェント及び
開発者が従う作業原則を定める。

作業を開始する前に、必ず `SCOPE.md` を読むこと。
`SCOPE.md` のGoal、Principles、MVP Scope、Acceptance Criteria及び
Change Controlを、すべての設計・実装判断に優先すること。

## 2. Goal-Oriented Development

- 既存のコード、データ、構成又は採用技術から要件を逆算しない。
- 既存実装との互換性、再利用又は改修を目的化しない。
- Goalからドメイン概念、利用者の判断フロー、データ契約及び受入条件を導く。
- 既存実装がGoalに適合する場合に限り、検証した上で再利用する。
- 既存構成を修正するより合理的な場合は、スクラッチから設計・実装する。
- 技術、クラウド、データベース、GIS製品及びAIサービスを前提条件としない。
- Goalに関わる重要な判断を、技術的都合によって縮小又は置換しない。

Goalだけでは重要なドメイン判断を確定できない場合は、推測して実装せず、
不足している判断事項と選択肢をユーザーへ提示すること。

## 3. 最初のMVP

最初のMVPは次を対象とする。

- 対象災害: 土砂災害
- 対象地域: 宮崎県延岡市及びその周辺
- 仮説の空間単位: 候補ポリゴン
- 主な利用者: 防災技術者及び関係機関
- レビュー結果:
  - `needs_additional_investigation`（追加調査が必要）
  - `monitor`（監視）
  - `dismissed`（棄却）

このMVPはGoal実現の最初の検証段階であり、最終Goalを土砂災害だけに
縮小するものではない。

## 4. 認識論上の原則

### 4.1 候補の位置付け

- 候補は `screening_candidate` であり、確定した危険区域ではない。
- 既存警戒区域外であることを、安全又は未知の危険の証明として扱わない。
- モデル出力を、観測事実、公式指定又は専門家判断と混同しない。
- スコアやランキングを、災害発生確率とみなさない。

### 4.2 根拠と情報不足

- 支持根拠と反証を分離して保持する。
- 公開情報の欠落を否定Evidenceとみなさない。
- NoData、未調査、取得不能及び品質不明を、ゼロ、false、安全又は非危険へ変換しない。
- 不確実性は `uncertainty` として記録する。
- 欠落情報は `missing_information` として記録する。
- 推定と事実を区別し、推定には手法、入力及び前提条件を付ける。

### 4.3 人間による判断

- 防災技術者又は権限を持つ関係機関が最終判断を行う。
- AI、統計モデル及びGIS解析は、判断材料の作成を支援する。
- 自動処理だけで災害の存在、公式指定、避難判断又は仮説の棄却を確定しない。
- レビュー結果には、判断理由、レビュアー及び日時を記録する。
- `dismissed` は、その時点で追加調査を進めない判断であり、安全の保証ではない。

## 5. 必須ドメイン情報

候補仮説について、少なくとも次を表現できるデータ契約を設計すること。

- 安定した仮説ID
- ジオメトリ、CRS及び対象地域
- 災害種別
- 仮説の説明
- epistemic status
- 生成手法、手法バージョン及びパラメータ
- 支持根拠
- 反証
- 不確実性
- missing information
- 情報源と来歴
- 既存警戒区域及び既知災害との関係
- 再現性の状態
- レビュー結果と判断履歴
- 現地調査又は事後検証の結果
- 仮説及び分析手法へのフィードバック

## 6. データと来歴

- 情報源、提供者、取得時点、版、ライセンス、空間範囲及び時間範囲を記録する。
- 原典と加工データを区別する。
- 加工データから原典と処理履歴を追跡できるようにする。
- 原データを前処理で上書きしない。
- データ品質、欠測、空間解像度、時間解像度及び適用限界を記録する。
- 公式情報は原典を優先し、複製する場合は原典との関係を保持する。
- 大容量データの複製を前提とせず、標準メタデータ、識別子及びAPIによる相互運用を優先する。
- 認証情報、秘密鍵、トークン及び個人情報をリポジトリへ保存しない。

## 7. アーキテクチャ原則

- ドメインモデルと外部技術を分離する。
- 仮説、Evidence、不確実性、情報源及びレビュー判断を中核概念として扱う。
- GIS、データベース、クラウド、AI及び可視化は交換可能な外部詳細として扱う。
- 入力、分析、仮説構築、レビュー及び出力の責務を分離する。
- 外部サービスが停止しても、保存済みの仮説と判断履歴を参照できる設計を検討する。
- 公式システムとの競合ではなく、標準形式及び明示的な契約による連携を目指す。
- 将来のマルチハザード及び複合・連鎖災害へ拡張できるが、MVPへ不要な抽象化を持ち込まない。

## 8. GIS及び分析実装

- 空間演算前にCRS、座標軸、単位、範囲及びジオメトリ妥当性を検証する。
- 距離及び面積は、目的に適した投影座標系で計算する。
- ラスタ結合前にshape、CRS、transform、解像度、bounds、band、dtype及びNoDataを検証する。
- NoDataマスクを分類、距離計算及び集計の最後まで保持する。
- 閾値には、値だけでなく根拠、単位、適用範囲及び版を持たせる。
- 候補IDは、同一入力と同一手法で安定して再生成できるようにする。
- 分析結果には、入力ID、処理時刻、手法バージョン、パラメータ及びソフトウェア版を付ける。
- 解析が完了したことだけを、妥当性確認又は科学的検証とみなさない。

## 9. AI利用原則

- AI生成内容をEvidenceそのものとして扱わない。
- 文献抽出、要約、分類又は説明生成にAIを使う場合は、原文への参照を保持する。
- AIが生成した推論と、原典に記載された事実を区別する。
- AI出力のモデル名、版、実行時点及び入力を可能な範囲で記録する。
- AIによる説明は、元データと決定ルールから検証可能であることを優先する。
- 専門家判断を誘導する断定的な表現を避ける。

## 10. テストと検証

分析ロジックには、少なくとも次の観点のテストを設けること。

- NoData及びmissing informationの伝播
- CRS及びグリッド不一致の検出
- 閾値境界
- 距離及び面積の単位
- ジオメトリ妥当性
- 安定した仮説ID
- 支持根拠、反証、不確実性及び情報源のシリアライズ
- レビュー状態遷移
- 既知リスク、区域外候補及び部分重複の代表例
- 同一入力と同一手法による再現性

地理空間出力について、feature数、有効pixel数、bounds、CRS、統計値、
カテゴリ数及び代表地点の目視確認を行うこと。

現地調査、既知災害又は専門家レビューと照合していない結果を、
検証済みと表現しないこと。

## 11. 作業手順

1. `SCOPE.md` を読む。
2. 依頼をGoal、Principles及びMVP Acceptance Criteriaへ対応付ける。
3. 必要なドメイン判断と未確定事項を明らかにする。
4. 既存実装を前提にせず、最小で妥当な設計を選ぶ。
5. 重要な判断が未確定なら、実装前にユーザーへ確認する。
6. 実装、テスト、地理空間検証及びドキュメントを一貫して更新する。
7. 仮定、未検証事項、残存リスク及びGoalへの寄与を報告する。

## 12. 完了条件

作業は、次を満たした場合に完了とする。

- 変更がGoalと承認済みMVPに対応している。
- 情報不足を安全又は否定Evidenceへ変換していない。
- 必要な来歴と認識論上の状態を保持している。
- 変更リスクに応じたテストと検証を実施している。
- 利用者が判断根拠と制約を確認できる。
- 未検証事項と残存リスクを明示している。

## 13. 変更管理

- Goalを変更、縮小、置換又は再解釈しない。
- Goal変更が必要な場合は、理由、期待効果、失われる価値及び影響を提示する。
- Goal変更にはユーザーの明示的な承認を必要とする。
- MVPの変更は、最終Goalとの関係を説明し、暗黙のGoal縮小を避ける。
